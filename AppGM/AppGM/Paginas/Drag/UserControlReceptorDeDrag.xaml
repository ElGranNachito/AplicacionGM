<UserControl x:Class="AppGM.UserControlReceptorDeDrag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppGM"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"

             local:ReceptorDeDragProperty.Value="True"
             
             Visibility="{Binding EsVisible, Converter={local:BooleanToVisibilityConverterColapsar}}">

    <!--ViewModel: ViewModelSeccionReceptoraDeDrag-->

    <Border Background="Transparent">

        <TextBlock 
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="40"
            Text="+"/>

    </Border>

</UserControl>
